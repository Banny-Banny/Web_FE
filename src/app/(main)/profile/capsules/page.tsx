/**
 * 캡슐보관함 페이지
 * 마이페이지에서 "캡슐" 클릭 시 진입
 */

'use client';

import { CapsuleStorage } from '@/components/CapsuleStorage';

export default function ProfileCapsulesPage() {
  return <CapsuleStorage />;
}
