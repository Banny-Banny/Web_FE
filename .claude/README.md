# Claude Desktop에서 Spec Kit 사용하기

이 폴더에는 Cursor와 동일한 Spec Kit 명령어들이 포함되어 있습니다.

## 📁 파일 구조

```
.claude/commands/
├── speckit.specify.md    # 기능 명세서 생성
├── speckit.plan.md       # 기술 계획 수립
├── speckit.tasks.md      # 작업 목록 생성
└── speckit.implement.md  # 구현 실행
```

## 🚀 사용 방법

Claude Desktop은 Cursor처럼 자동으로 commands를 인식하지 않으므로, 다음과 같이 사용하세요:

### 방법 1: 직접 명령어 참조
Claude Desktop에서 작업할 때, 필요한 명령어 파일의 내용을 참조하여 사용하세요:

1. **기능 명세서 생성**: `.claude/commands/speckit.specify.md` 파일의 내용을 참조
2. **기술 계획 수립**: `.claude/commands/speckit.plan.md` 파일의 내용을 참조
3. **작업 목록 생성**: `.claude/commands/speckit.tasks.md` 파일의 내용을 참조
4. **구현 실행**: `.claude/commands/speckit.implement.md` 파일의 내용을 참조

### 방법 2: 프롬프트에 파일 내용 포함
Claude Desktop에서 다음과 같이 요청하세요:

```
"프로젝트의 .claude/commands/speckit.specify.md 파일을 읽고, 
이 가이드에 따라 새로운 기능의 명세서를 생성해줘"
```

### 방법 3: 컨텍스트로 추가
Claude Desktop의 컨텍스트에 `.claude/commands/` 폴더를 추가하여 
항상 명령어 가이드를 참조할 수 있도록 설정하세요.

## 📝 Spec Kit 워크플로우

1. **speckit.specify** → 기능 명세서(`spec.md`) 생성
2. **speckit.plan** → 기술 계획(`plan.md`) 수립
3. **speckit.tasks** → 작업 목록(`tasks.md`) 생성
4. **speckit.implement** → 실제 구현 진행

## 🔄 동기화

`.cursor/commands/` 폴더의 내용이 업데이트되면, 
다음 명령어로 `.claude/commands/`도 동기화할 수 있습니다:

```bash
cp -r .cursor/commands/* .claude/commands/
```

또는 심볼릭 링크를 사용하여 항상 동기화 상태를 유지할 수 있습니다:

```bash
rm -rf .claude/commands
ln -s ../.cursor/commands .claude/commands
```

---

**참고**: Cursor와 Claude Desktop 모두에서 동일한 Spec Kit 워크플로우를 사용할 수 있습니다.
